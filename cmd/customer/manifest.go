package main

const EnvPrefix = ""

var (
	// AppSlug Application Slug Name
	AppSlug = "ms-customer"
	// AppName Application Name
	AppName = "PDS Microservice Customer"
	// AppVersion it will be override during automated build
	AppVersion = "0.0.0-local"
	// BuildSignature value will be overridden during automated build
	BuildSignature = "local.build"
)
